package Exec.Root.Modules;

import java.util.HashMap;

public interface RootCode_Database {
	/*
	 * Get method's name by its alias
	 * Returns null if method doesn't exists or access denied
	 */
	public default void Init_Database(HashMap<String,String> map) {
/*
// База данных
НачатьТранзакцию
ЗафиксироватьТранзакцию
ОтменитьТранзакцию
*/
	}
}
