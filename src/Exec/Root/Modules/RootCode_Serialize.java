package Exec.Root.Modules;

import java.util.HashMap;

public interface RootCode_Serialize {
	/*
	 * Get method's name by its alias
	 * Returns null if method doesn't exists or access denied
	 */
	public default void Init_Serialize(HashMap<String,String> map) {
/*
// —истемные
_IdToStr
_StrToId
«начение¬—трок¬нутр
«начение»з—троки¬нутр
«начение¬—троку
«начение»з—троки
«начение¬‘айл	(им€ файла, объект, [формат (1=внутр, иначе=внешний)=1])
«начение»з‘айла	(им€ файла, [объект], [формат (1=внутр, иначе=внешний)=1])
—охранить«начение
¬осстановить«начение 
*/
	}
}
