package Exec.Root.Modules;

import java.util.HashMap;

public interface RootCode_Format {
	/*
	 * Get method's name by its alias
	 * Returns null if method doesn't exists or access denied
	 */
	public default void Init_Format(HashMap<String,String> map) {
/*
// Форматирование
Пропись		(имя файла с прописью или список значений той же структуры)
Формат		(параметр, форматная строка)
Шаблон
ФиксШаблон
*/
	}
}
