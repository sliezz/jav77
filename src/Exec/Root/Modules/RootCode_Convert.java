package Exec.Root.Modules;

import java.util.HashMap;

public interface RootCode_Convert {
	/*
	 * Get method's name by its alias
	 * Returns null if method doesn't exists or access denied
	 */
	public default void Init_Convert(HashMap<String,String> map) {
/*
// Типы
Дата	(пар)
Дата	(Год, Месяц, Число)
Строка	(пар)
Число	(пар)
*/
	}
}
